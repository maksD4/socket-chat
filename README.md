## Topic
Instant messaging. An application that allows for client-to-client interactions.

## Requirements
The instant messaging application allows for account creation, password-based login, friend display, multiple simultaneous conversations, and group conversations between at least three users. User data is stored via the application. A GUI is required.